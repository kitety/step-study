<view>
  <v-epsoide index="{{classic.index}}"/>
  <v-like count="{{classic.fav_nums}}" like="{{classic.like_status}}" bind:like="likeClick" />
  <v-movie img="{{classic.image}}" content="{{classic.content}}"></v-movie>
</view>
